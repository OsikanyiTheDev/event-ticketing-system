# Dev tooling (local development + CI)
pytest==8.3.4
pytest-cov==6.0.0
ruff==0.8.6
boto3==1.35.94        # AWS SDK — pinned close to Lambda runtime
botocore==1.35.94
moto==5.0.21          # AWS mock for unit-testing Lambda/DynamoDB locally
